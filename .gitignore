# Dependencies
node_modules/

# Environment
.env

# Uploaded files
uploads/

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Ansible secrets - only the ENCRYPTED vault.yml should ever be committed
# (ansible-vault encrypt turns it into ciphertext safe for git; never commit it in plaintext)
ansible/*.retry

# Local AWS credential files
aws-credentials.local
.aws/credentials
